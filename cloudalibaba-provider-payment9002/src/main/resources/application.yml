server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: 169.254.95.102:8848 #配置Nacos地址

#端口暴露
management:
  endpoints:
    web:
      exposure:
        include: '*'
